rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Lecture publique pour tous
    match /{document=**} {
      allow read: if true;
    }
    
    // Écriture bloquée (sauf functions)
    match /{document=**} {
      allow write: if false;
    }
  }
}
